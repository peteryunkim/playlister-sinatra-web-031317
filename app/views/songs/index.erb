<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SONG LIST</title>
  </head>
  <body>
    <h1>SONG LIST</h1>
    <%Song.all.each do |song|%>
      <a href="/songs/<%= song.slug %>">
        <%=song.name%></a>
      <%end%>
  </body>
</html>
