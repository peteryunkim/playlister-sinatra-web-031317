<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/songs/" method="post">
      song name
      <input id = "Name" type="text" name="song[name]">

      <br></br>
      <% Genre.all.each do |genre| %>
        <%= genre.name%>
        <input type="checkbox" name="song[genre_ids][]" id="<%= genre.name %>" value = "<%= genre.id %>"></input>
      <%end%>
      <br></br>
      new genre
      <input type="text" name="genre[name]">
      <br /><br />

      <label>select artist</label>
      <%Artist.all.each do |artist|%>
      <input type="radio" name="song[artist_id]" value="<%=artist.id%>" id="<%=artist.name%>"><%=artist.name%></input>
      <br></br>
      <%end%>
      <br></br>
      <label>Or create new artist</label>
      <input id="Artist Name" type="text" name="artist[name]" value="">


      <input type="submit" value="Create">
    </form>
  </body>
</html>
